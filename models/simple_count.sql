select 
    1 as id,
    'test' as category,
    current_date as created_date
union all
select 
    2 as id,
    'test' as category,
    current_date as created_date
union all
select 
    3 as id,
    'demo' as category,
    current_date as created_date