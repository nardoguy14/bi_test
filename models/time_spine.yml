version: 2

models:
  - name: time_spine
    description: "A time spine with one row per day, ranging from 2020 to 2030"
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "The base date column for daily granularity"
        granularity: day